# Cursor Coding Models — Tiered Picks

Best pick per tier, plus 2 alternates. "Fast" = best model among the fast ones, not just the quickest.

## 1. Fast tier — best of the fast
1. **`claude-opus-4-7-medium-fast`** — strongest reasoning still in the fast lane
2. `gpt-5.3-codex-fast` — code-tuned, very snappy
3. `composer-2-fast` — Cursor default, lowest latency

## 2. Medium tier — daily driver
1. **`claude-opus-4-7-high`** — solid multi-file work, debugging, tests
2. `gpt-5.3-codex-high` — strong pure-code option
3. `gpt-5.5-medium` — good generalist with 1M context

## 3. Good tier — hard problems, architecture
1. **`gpt-5.3-codex-xhigh`** — top-end Codex for gnarly code
2. `claude-opus-4-7-xhigh` — long-context heavy lifting
3. `gpt-5.5-extra-high` — wide-context reasoning

## 4. Best of the best — when all else fails
1. **`claude-opus-4-7-thinking-max`** — max reasoning + extended thinking, 1M ctx
2. `gpt-5.3-codex-xhigh` — fallback when Opus loops on pure-code problems
3. `claude-opus-4-7-max` — Opus max without thinking, faster than `thinking-max`
