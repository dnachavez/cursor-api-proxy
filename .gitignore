node_modules/
dist/
# Cursor / Claude local IDE settings
.claude/
*.log
.env
.DS_Store
# Tailscale / TLS certs (keep private keys out of repo)
*.ts.net.crt
*.ts.net.key
# Session/request log (optional; path configurable via CURSOR_BRIDGE_SESSIONS_LOG)
sessions.log
logs/
# Local Cursor rules (no-pii etc.) — keep out of public fork
.cursor/
# SwiftPM build artifacts (widget)
widget-mac/.build/
